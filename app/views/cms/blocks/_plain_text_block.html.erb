<%# Heading + wide copy, no image. %>
<section class="w-full py-section">
  <div class="page-container">
    <h3 class="heading-section mb-6 lg:mb-10"><%= cms_heading(block.title) %></h3>
    <div class="text-body lg:max-w-[1226px] cms-rich-text"><%= cms_body(block.content) %></div>
  </div>
</section>
